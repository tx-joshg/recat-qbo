# Decisions log — judgment calls made during the build

Items here were not fully specified in the design docs/specs; each records the call made
and why, for later review. (Handoff doc §6 decisions are restated in specs and not repeated here.)

| # | Decision | Rationale | Revisit? |
|---|---|---|---|
| 1 | ~~Roles are instance-wide~~ **REVISED (owner request): roles are per-company memberships.** `Membership(userId, companyId, role)` + `User.isInstanceAdmin` (manages settings/users/connections; implicitly admin in every company). Company visibility = membership; the Settings Team card manages the active company's members with an inline role select (deviation from the prototype's static text). Only instance admins connect/disconnect companies. | Owner review 2026-07-16; matches specs.md §2's original per-company design. | — |
| 2 | **`QBO_MOCK=true` demo mode** ships in the server: a mock QuickBooks realm with the prototype's sample data (2 companies, chart of accounts, transactions). OAuth, sync, write-back, transfers all work against it. | The goal requires a fully working local app; sandbox credentials can't be assumed. Also gives OSS evaluators a zero-setup demo (specs Phase 3 "seed/demo mode"). | Keep; document prominently. |
| 3 | ~~Splits as Json column~~ **REVISED (owner request): splits are a `SplitLine` child table** (+ `SplitLineTag`), and every report attributes each line's own amount to its own category and tags — a $150 split of $125/$25 shows $125 and $25 under their respective category/tag rows, never the whole txn. API DTO shape unchanged. | Owner review 2026-07-16 — split-accurate reporting became a first-class requirement. | — |
| 4 | **Sessions in DB** (hashed token, 30-day expiry, revocable) rather than stateless cookies. | Magic-link auth with sign-out and invite revocation wants server-side sessions; table is trivial. | — |
| 5 | **Category stored as both display name and QBO account id** on Transaction/Rule. | Handoff models use display names (UI contract); write-back needs `AccountRef.value`. Storing both avoids name-resolution drift. | — |
| 6 | **Suggestion computed at read time** (rules → history → AI) rather than persisted at sync, except AI responses which are cached per payee in `AppConfig`-style cache table? → implemented as in-memory + `Transaction.suggestion` snapshot refreshed on sync. | Rules are editable at any moment and must win instantly (prototype behavior: editing a rule immediately re-labels queue rows). History likewise changes after each post. | — |
| 7 | **Transfer detection window: ≤3 days apart** per handoff §2; prototype ignores dates (demo data all close together). Implemented with the 3-day rule. | Handoff is authoritative. | — |
| 8 | **Undo window 30 days** enforced server-side; UI always shows ↩ on POSTED/DRY_RUN (prototype has no age gate visible). | Handoff §2/§6. | Config-worthy later per handoff. |
| 9 | **Magic links print to server log when SMTP is unconfigured** (dev/demo). Response never reveals whether the email exists (no user enumeration, handoff §4). | Local-first development; passwordless flow must be exercisable without SMTP. | — |
| 10 | **Dashboard financials computed from synced + posted data in mock/demo mode use the prototype's demo series** so the screens match the design pixel-for-pixel; in real mode they are computed from QBO report data at sync. Real-mode P&L/BS come from local aggregation of synced transactions and account balances (QBO Reports API is a Phase-later enhancement). | Prototype shows rich historical series a fresh install can't have; the demo seed reproduces them. Real deployments accumulate their own history. | Consider QBO Reports API for full-fidelity statements. |
| 11 | **Webhook endpoint is instance-global** (`POST /webhooks/qbo`) and fans out to companies by realmId in the payload. | Intuit sends one webhook per app, not per company. | — |
| 12 | **Nightly reconcile at 2:00 AM server time** (specs) marks txns categorized inside QBO as SUPERSEDED and refreshes reference data. | specs.md §3. | — |
| 13 | **AGPL-3.0-only** license. | CLAUDE.md/specs both call for AGPL to prevent closed-source hosted resale. | — |
| 14 | ~~Density not exposed~~ **REVISED (owner request): Density card in Settings** (Comfortable/Compact toggle, per-user per-browser via localStorage, drives the prototype's `data-density` CSS vars). Card styled after the tags-required card since the design has no surface for it. | Owner review 2026-07-16. | — |
| 15 | **CSRF**: same-site=lax session cookie + JSON-only bodies + origin check middleware on mutating routes. | Standard for cookie-session SPAs; specs silent. | — |
| 16 | **Bulk post caps at QBO batch limit** by chunking into 30-op batch requests (real mode); mock mode posts sequentially. | QBO constraint from CLAUDE.md. | — |
| 17 | **Viewer role default landing page is Dashboard** (viewers can't see Queue). | Handoff §5: viewer sees only Dashboard + Reports. | — |
| 18 | **Wizard step 1 (admin account) creates the admin user immediately and signs them in via the same magic-link flow**; in mock/dev mode the link is one-click from the UI like the prototype's "Prototype shortcut". | Prototype shows a shortcut; production needs real verification. | — |
| 19 | **No Tailwind.** Styling is the prototype's CSS custom properties + a small set of utility classes + inline styles with verbatim values. | The handoff demands pixel-perfect fidelity; the prototype is inline-styles + CSS vars. Translating to Tailwind adds drift risk with zero user benefit. Deviates from CLAUDE.md's stack table row. | Migrate later if the team prefers. |
| 20 | **Transfers post as recategorizations, not a QBO `Transfer` entity**: each leg's holding-account line is recategorized to the other leg's bank account — which is exactly what a transfer is in double-entry. Avoids creating a duplicate Transfer entity on top of two already-posted bank transactions. | The two legs already exist as posted txns; adding a Transfer entity would double-book. Audit action is still `transfer`. | If accountants prefer true Transfer entities + voiding the legs, revisit. |
| 21 | **Token/secret encryption format**: `base64(iv).base64(tag).base64(ciphertext)` AES-256-GCM; undecryptable stored values (key rotation) are treated as unset with a logged error rather than crashing. | Self-describing, tamper-evident; graceful key rotation. | — |
| 22 | **Daily digest has no UI** — configured purely via env (SMTP/SLACK_WEBHOOK_URL, DIGEST_HOUR), sent to all non-pending users when the queue is non-empty. | The design prototype's Settings screen has no digest section; specs call for the feature. Env-only keeps the pixel-faithful UI intact. | Add per-user opt-out UI when designed. |
| 23 | **Payee normalization for history suggestions**: uppercase, `#`/`*`/`·` as separators, tokens containing digits dropped (`SYSCO FOODS #212` → `SYSCO FOODS`). | Groups store-numbered payees without a vendor database. | — |
| 24 | **JournalEntries that *credit* a holding account are out of scope v1** (debit-side JEs work). | Rare shape; documented in code. | — |
| 25 | **Multi-line entity safety**: a synced transaction's amount is the sum of its *holding-account lines only* (never `TotalAmt`), and write-back replaces only holding lines — every already-categorized or funding line survives verbatim. | Adversarial review found the naive approach could silently change entity totals or wipe existing categorizations on multi-line Purchases/Deposits/JEs. | — |
| 26 | **Splits must be nonzero and share the transaction's sign**; enforced at both the API schema and the write-back guard. | Mixed-sign splits summing correctly could otherwise inflate the QBO entity total. | — |
| 27 | **Transfers commit per-leg**: leg A's QBO write + status + audit commit atomically before leg B starts; a leg-B failure leaves A honestly POSTED and marks B ERROR with an audit row. | A QBO write with no audit trail is the one unforgivable state. | — |
| 28 | **Auto-post rules never touch a transaction a human has staged** (category/splits/tags present), and one failing auto-post can't abort a sync run. | Silent overwrite of human work is this app's worst failure mode. | — |
| 29 | **Stuck POSTING sweep**: rows in POSTING >5 min flip to ERROR ('server restarted mid-post…') at boot and every scheduler tick. | Crash mid-post must not strand queue rows on an eternal spinner. | — |
| 30 | ~~devLink requires QBO_MOCK=true or ALLOW_DEV_LOGIN=true~~ **devLink policy revised by #42 (auto-lock on real connection).** Still true: production boot refuses default SESSION_SECRET/ENCRYPTION_KEY (now unconditionally); webhooks 401 without a verifier token. | NODE_ENV inference alone left a takeover hole for careless deployments. | — |
| 31 | **Mock realm state persists in the DB** (`AppConfig mock:realm:<realmId>`, write-through) so demo mode stays coherent across server restarts and seed runs; disabled under tests. After truncating the DB for a reseed, restart the dev server (its in-memory copy is stale). | Undo of a posted demo txn failed after a restart because the in-memory fake QuickBooks forgot its mutations. | — |
| 32 | **Sync uses upsert + a per-company in-flight mutex; CDC falls back to a full sweep** when it errors or lastSyncedAt >25 days (Intuit's CDC cap is 30). All QBO queries paginate (STARTPOSITION), and SUPERSEDED is only ever marked against a complete sweep. | Correctness under concurrency, downtime, and >1000-entity books. | — |
| 33 | **Rule priority (owner request)**: rules carry an integer priority, lowest wins, `createdAt desc` tiebreak; new rules created at the top (`min−1`, gaps allowed); Rules screen gets a ⋮⋮ drag handle (desktop-only for now) and `PUT /rules/order` persists 0..n−1. The design prototype had no priority UI — extension styled to match. | Multiple rules matching one payee needed explicit, user-controlled precedence (was implicit newest-first per handoff §3). | Mobile reordering later. |
| 34 | **Undo ↩ hidden after 30 days** in the queue UI (server already rejected; the button would only error). Null postedAt stays undoable. | Owner question surfaced the mismatch. | — |
| 35 | ~~Demo→real transition via boot flag~~ **REVISED (owner request, see #42): demo and real companies coexist.** The startup auto-disconnect of demo companies is gone; users disconnect demo companies from Settings when done. `QBO_MOCK` no longer flips anything at boot. | Owner complaint — the boot-time flag silently overrode the user's in-wizard choice (real credentials + Production still landed on the fake consent screen). | — |
| 36 | **No bundled mail server**, but **SMTP is now configurable in-app** (owner request): a wizard Email step (skippable) and an admin Settings card capture host/port/user/pass/from (password encrypted at rest) with a Send-test-email button; env vars still win as a block when `SMTP_HOST` is set. | Bundled relays don't reach real inboxes reliably; in-app config removes the env-var hurdle for new users. | — |
| 37 | **Rule transparency (owner request)**: the queue's `rule` badge tooltips "Matched N rules — '{winner}' won (topmost)" when several rules match; the Rules screen has a Test button that dry-runs a draft matchText against the 200 newest pending/posted transactions and lists conflicts with existing rules. Design extension — no prototype surface existed. | Users need to see and predict rule interactions before trusting auto-categorization. | — |
| 38 | **Real-mode P&L/Balance Sheet come from QuickBooks' Reports API** (drift-free by construction), parsed from Intuit's report JSON; 60s cache per statement for rate limits. Statement line rows are click-to-drill: an inline section shows the row's underlying transactions (QBO TransactionList report in real mode; the local mirror in demo mode). Demo statements keep the design prototype's synthetic series — demo drill-down only shows mirrored sample transactions, so synthetic rows can read empty (demo artifact). The Custom & tags report remains locally computed (tags are Recat-only; QBO cannot compute them). | Owner review 2026-07-16 — local aggregation would drift from QBO for any business with activity outside the holding accounts, and drilling into a statement shouldn't require opening QuickBooks. | — |
| 39 | **Mobile nav is a hamburger menu (owner request)**: ≤640px the bar shows logo + dry-run pill + ☰; the menu holds the tabs (role-gated), company switcher, theme toggle, and account/sign-out. Deviates from the prototype's wrapped-nav mobile treatment. Mobile sweep also fixed fixed-width login/wizard overflow, statement-card internal scrolling, team/rules/tags row collapses, bulk-bar column layout, and code-chip wrapping. | Owner review 2026-07-16 — the wrapped nav ate a third of a phone screen. | — |
| 40 | **Tooltip hardening**: `data-tip` bubbles gained `down` and `right`-aligned position variants (nav tooltips clipped above the viewport; rightmost-column tooltips clipped at the right edge), `:active`/`:focus-visible` triggers as a touch/keyboard affordance, and viewport-aware max-width. Toast and rule-prompt bars cap at `100vw − 24px` and wrap. | Owner asked for tooltip/toast verification; audit found real clipping bugs. | — |
| 41 | **Project name: Recat QBO** (owner decision, 2026-07-16). Repo `recat-qbo`; root package + Docker image renamed to match; README header, GitHub description, and topics per the owner's text (see docs/publishing.md). The in-app brand remains "Recat" — the logo and UI copy stay pixel-faithful to the design prototype; "QBO" is a discovery suffix, not chrome. Internal `@recat/*` workspace scopes unchanged (never published). | Keeps the shipped visual identity while making the public name self-describing and searchable. | — |
| 42 | **Demo vs real is a user choice made in the UI and honored — not a boot-time env var (owner request, 2026-07-16).** (a) `qboFactory.forCompany` dispatches per company: MockQboClient when `realmId` is a mock realm, RealQboClient otherwise — `QBO_MOCK` no longer selects the factory. (b) Connect endpoints take the choice explicitly (`GET /api/companies/connect-url?mode=real\|demo&env=sandbox\|production`); the per-connection env rides on the OAuth state token and wins over the AppConfig/env default; `mode=real` without credentials 400s with "Add your Intuit Client ID and Secret first — Settings → QuickBooks API access". (c) The wizard gained a first **Start** step (Try the demo / Connect my real QuickBooks); the rail reflects the chosen path (demo skips Credentials); the old amber "demo mode is on" banner is gone. (d) Demo connections through the callback install the demo financial series (`services/demoFinancials.ts`, shared with seed.ts) so hosted demos get full dashboards. (e) devLink auto-locks: allowed iff `ALLOW_DEV_LOGIN=true` OR no real company is connected (`services/devLogin.ts`); flips live as companies connect/disconnect. (f) `QBO_MOCK` demoted to local dev: it gates only the seed and the webhook verifier bypass; production boot ALWAYS enforces real secrets; the mock-consent route is always mounted (state token still required); the startup demo-disconnect is removed — demo/real coexist, demo companies wear a `demo` badge in Settings and the company switcher. `/connect` re-enters the wizard's Connect step in real mode when credentials exist, demo otherwise. | The owner entered real Intuit credentials, picked Production, and was silently routed to the fake consent screen because the deployment had `QBO_MOCK=1`. The user's explicit choice must win end to end. | — |
| 43 | **Transaction log rows are always taggable and always dated with a year (owner report, 2026-07-17).** (a) Log dates render as `Jul 13, 2026` (new `fmtDateY`); everywhere else `fmtDate` now appends the year automatically when a date is outside the current year, so old queue/rules rows self-identify how far back they are. (b) Rows QBO returns without an entity id (real TransactionList reports may omit ColData ids) get a synthetic tag key `row:<sha256(date\|type\|doc\|payee\|amount)[..40]>` instead of silently losing the "+ tag" button; `qboKey` is now non-optional on `TransactionLogRowDto`. Identical rows share a key — tagging one tags all — the honest tradeoff when QBO gives nothing to tell them apart. | Owner couldn't tell how far back the log went (year-less dates) and saw no place to add tags on their real-books connection. | If Intuit ever guarantees ids on TransactionList rows, drop the fallback. |
